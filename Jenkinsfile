pipeline {
    agent any
    
    stages {
        
        stage('github-clone') {
            steps {
              //  git branch: 'BE', credentialsId: 'github_token', url: '{REPOSITORY URL}'
                echo 'Building the project...'
            }
        }
        
   		// stage...
   	}
}
